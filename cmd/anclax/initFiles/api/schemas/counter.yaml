schemas:
  Counter:
    type: object
    required: [count]
    properties:
      count:
        $ref: ./common.yaml#schemas/Amount
  IncrementCounterParams:
    type: object
    required: [amount]
    properties:
      amount:
        $ref: ./common.yaml#schemas/Amount
